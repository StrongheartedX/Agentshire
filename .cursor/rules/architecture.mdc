---
description: 小镇前端和 Bridge 层的架构约束，所有涉及动画编排、NPC 行为、事件分发的修改必须遵守
globs:
  - town-frontend/src/**
  - src/bridge/**
alwaysApply: false
---

# Agentshire 架构规则

## 层级职责

### Bridge 层（src/bridge/）
- **只做**：Agent 生命周期管理、Phase 状态机、高级意图事件发射（`workflow_*`、`npc_work_done`）
- **不做**：setTimeout/scheduleDelayedEmit 微操编排动画序列、直接发 `npc_anim`/`npc_phase`/`npc_emoji` 等帧级指令来编排视觉效果
- Bridge 不知道前端的动画时长、NPC 位置、场景归属——不要用硬编码时间假装知道

### MainScene（town-frontend/src/game/MainScene.ts）
- **只做**：子系统注册、update 循环、事件路由（一行委托到对应子系统）
- **不做**：超过 5 行的 async 编排逻辑、直接操作 NPC 动画序列
- 所有 EventDispatcher handler 应该是一行委托，例如：`onXxx: (args) => this.workflow.handleXxx(args)`

### workflow/ 目录（town-frontend/src/game/workflow/）
- **定位**：办公工作流的动画编排
- 所有涉及 NPC 动画序列的编排逻辑放在这里（WorkflowHandler、各 Orchestrator、未来的 Choreographer）
- `WorkflowHandler` 同时负责状态存储（officeNpcStations、进度等）和编排方法（handleNpcWorkDone、startOfficeWork 等）

### NPC Entity（town-frontend/src/npc/NPC.ts）
- NPC 动画通过**状态机** `transitionTo(state)` 驱动，不直接操作底层动画
- `playAnim()` 是向后兼容层，内部映射到 `transitionTo()`——新代码应直接使用 `transitionTo()`
- `moveTo()` 内部通过状态机切换到 `walking` 状态，自动保证 walk 动画
- LoopOnce 动画（celebrating/emoting）播完后通过集中 `finished` 回调自动回 idle
- 状态转换有严格规则（`STATE_TRANSITIONS`），非法转换会 warn 并回退到 idle

## 事件流约束

### Bridge → 前端：高级意图，非微操
```
正确：emit({ type: 'npc_work_done', npcId, status, stationId })
错误：emit({ type: 'npc_anim', npcId, anim: 'cheer' })
      scheduleDelayedEmit(2000, [{ type: 'npc_emoji', npcId, emoji: null }])
      scheduleDelayedEmit(3000, [{ type: 'npc_move_to', ... }])
```

### 前端 → Bridge：完成回传
- 前端编排完成后通过 `dataSource.sendAction({ type: 'workstation_released', ... })` 或 `workflow_phase_complete` 通知 Bridge
- Bridge 收到回传后才推进 Phase 状态机或释放资源

### 工位释放
- Bridge 在 `sub_agent done` 时**不立即释放工位**
- 前端 NPC 物理离开工位后通过 `workstation_released` action 通知 Bridge
- Bridge 有 12 秒安全网 setTimeout 兜底

## 新增编排序列的正确做法

1. 在 `workflow/` 下对应的 Handler 或 Orchestrator 中添加 async 方法
2. 在 `EventDispatcher.ts` 中添加新事件类型的 dispatch case
3. 在 `MainScene.ts` 中添加一行委托到 workflow
4. 如果需要 Bridge 触发，在 Bridge 中发高级意图事件，不要用 setTimeout 编排
5. 更新 `town-frontend/src/game/AGENTS.md` 和 `src/bridge/AGENTS.md`
